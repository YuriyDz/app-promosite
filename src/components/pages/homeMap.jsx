/*function conwertWithObjectToMas(obj){
    let arr = [];
    for(let j in obj){
        let arrPush = [];
for(let i in obj[j]){
arrPush.push(obj[j][i]);
}
arr.push(arrPush);
}
console.log(arr);
return Array(arr);
}
export const Items=({item,func})=>{
    let obj = item;
    let arr = [];
    for(let j in obj){
        let arrPush = [];
for(let i in obj[j]){
arrPush.push(obj[j][i]);
}
arr.push(arrPush);
}
console.log(arr);

   console.log(typeof(arr/*);
    arr.map((value,index) => (
//<button onClick={()=>func(value.id)}>
<>
<p>{value["name"]}</p>
<p>{value["dateToOpen"]}</p></>
//</button>
    ));
return arr;
};*/